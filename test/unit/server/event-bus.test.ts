/* eslint-disable @typescript-eslint/no-unused-vars */

import { describe, it, expect } from 'vitest';

import { EventBus } from '../../../src/server/events';

describe('EventBus', () => {
  it.todo('should emit events to registered handlers');
});
