/* eslint-disable @typescript-eslint/no-unused-vars */

import { describe, it, expect } from 'vitest';

import { encodeFrame, decodeFrame, Opcode } from '../../../../src/shared/protocol';

describe('frames', () => {
  it.todo('should encode and decode binary frames');
});
