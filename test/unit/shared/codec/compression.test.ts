/* eslint-disable @typescript-eslint/no-unused-vars */

import { describe, it, expect } from 'vitest';

import { compress, decompress } from '../../../../src/shared/codec';

describe('compression', () => {
  it.todo('should compress and decompress data');
});
