/* eslint-disable @typescript-eslint/no-unused-vars */

import { describe, it, expect } from 'vitest';

import { RpcClient } from '../../../src/client/rpc';

describe('RpcClient', () => {
  it.todo('should generate unique correlation IDs');
});
