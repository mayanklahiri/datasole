{
  "name": "datasole-demo-vanilla",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('Vanilla demo serves source files directly; no build step required.')\"",
    "dev": "node --watch server/index.mjs",
    "start": "node server/index.mjs",
    "clean": "rm -rf node_modules"
  },
  "dependencies": {
    "datasole": "file:../.."
  }
}
