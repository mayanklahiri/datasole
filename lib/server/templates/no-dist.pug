html
  head
    title= 'HTTP 500: ' + error
    include ./style.pug
  body
    h1 HTTP 500: Internal Error
    h3 There was an internal error serving the web application.
    h4 Error
    code= ' ' + error
    h4 Stack
    pre= stack