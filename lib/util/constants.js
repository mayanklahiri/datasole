module.exports = {
  /**
   * Environment variable name prefix to mark Datasole options.
   */
  ENV_VAR_PREFIX: "datasole_",

  /**
   * Regex to describe an integer string.
   */
  RE_INTEGER: /^[\d+]$/
};
